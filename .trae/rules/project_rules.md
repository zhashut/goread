1、记住我们的项目的架构是 Tauri V2 (React/Rust)，做的是移动端（android 和 ios） 
2、我们 app 会支持多种书籍格式，在处理每种书籍格式逻辑时，不要修改到其他格式的逻辑，只修改当前格式的逻辑，每个格式的逻辑都要独立处理，不能相互干扰，有共用的逻辑要抽离出来，放到一个公共的函数或模块中，每个格式的逻辑都要调用这个公共的函数或模块，不能各自实现相同的功能，谨记！
